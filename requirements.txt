harmonica>=0.7,<0.8
ppigrf>=2.1,<3
defusedxml>=0.7,<1
# Complete open-source 3D inversion stack. Declared here because QPIP only
# discovers the canonical requirements.txt file during plugin installation.
simpeg>=0.25,<0.26
discretize>=0.12,<0.13
choclo>=0.3,<0.4
